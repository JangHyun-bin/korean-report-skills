# 변경 이력

[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르며
[유의적 버전](https://semver.org/lang/ko/)을 쓴다.

## [1.6.0] — 2026-08-10

### 더해짐

- **`scripts/new-document.py`** — 새 문서 생성기의 뼈대를 만든다.
  만들어진 `.py` 는 **그 자체로 생성 → mathbuild → QA 를 한 번에 돈다.**
  `SKILL.md` §2 를 처음부터 읽지 않아도 첫 문서가 나오게 하는 것이 목적이다.
  이미 있는 파일은 덮어쓰지 않는다.

  ```bash
  python scripts/new-document.py --title "문서 제목" --mode paper
  python 문서_제목.py
  ```

- 스캐폴드가 자기 QA 를 통과하는지 검사한다. 처음 쓰는 사람이 가장 먼저
  실행하는 것이라 여기서 실패하면 첫인상이 실패로 시작한다.
  실제로 deck 쪽 치환 자리 수가 어긋나 캡션이 밀린 것을 이 검사가 잡았다.

---

## [1.5.0] — 2026-08-10

### 더해짐

- **deck 인쇄 오버플로 검사** — 타일이 한 쪽(A4 가로 210mm)을 넘으면 `qa.py` 가
  실패하고 어느 타일이 몇 px 넘쳤는지 알린다. `.tile` 은 인쇄 시 `overflow:hidden`
  이라 넘친 내용이 **조용히 잘린다** — deck 에서 가장 흔한 실패인데
  유일하게 자동 검사가 없던 자리다. 일부러 과밀한 타일을 만들어 잡히는 것을 확인하였다.
- **GitHub Pages** — 예시 문서를 실물로 공개한다. 스크린샷과 달리 파일 하나로
  완결된다는 것을 소스 보기로 직접 확인할 수 있다.
  https://janghyun-bin.github.io/korean-report-skills/

### 고침

- `qa.py` 가 **파일명으로 모드를 추측**하던 문제. `deck` 이 이름에 없는 파일은
  paper 로 취급되어 인쇄 검사가 건너뛰어졌다. 이제 `<html data-mode>` 를 읽고,
  값이 없거나 알 수 없으면 실패한다.

---

## [1.4.0] — 2026-08-10

공개 준비 마무리 — 라이선스 고지와 저장소 위생.

### 더해짐

- **생성 문서가 내장 글꼴의 라이선스를 스스로 고지한다.** 글꼴을 base64 로 넣는 것은
  그 사본을 문서와 함께 배포하는 것이므로 OFL·MIT 모두 고지 동반을 요구한다.
  KaTeX(MIT)는 항상, Pretendard(OFL 1.1)는 `--font` 로 내장했을 때만 실린다.
- `SECURITY.md` — 이 저장소의 특수한 위험(에이전트가 읽고 따르는 지시문)을 명시하고
  비공개 신고 경로를 안내한다. 설치자가 직접 확인할 수 있는 명령도 함께 적었다.
- 이슈 템플릿 2종 — 결함 신고, 규약 제안. 규약 제안 템플릿은
  「판정법으로 잡히는지 먼저 확인」을 첫 안내로 둔다. 표를 무한히 늘리지 않기 위해서다.
- PR 템플릿 — 「무엇을 왜」와 확인 목록. 다크 타일 육안 확인 항목을 포함한다.
- **버전 동기화 검사** — `package.json` · `pyproject.toml` · `CHANGELOG` 최신 항목이
  같은지 확인한다. 지금까지 우연히 맞아 있었을 뿐 검사가 없었다.
  릴리스 태그가 어긋나면 잘못된 버전이 배포된다.
- 저장소 위생 파일 존재 검사.

### 바뀜

- **금지 실명을 평문으로 두지 않는다.** 금지어 목록에 이름을 적으면 그 목록 자체가
  노출이다. 낱말 sha256 해시로 대조하도록 바꾸고, 이름을 몰라도 잡히도록
  법인 표기(`㈜` · `주식회사` · `Ltd.`) 형태 검사를 함께 두었다.
  검사 범위는 `LICENSE` · `NOTICE`(저작권 표기가 정상인 곳)를 제외한 전부다 —
  루트 문서를 빼면 거기로 실명이 새어 들어온다.
- 문서에 남아 있던 실측 수치 리터럴을 일반 표현으로 교체하였다.
  「소수점 백분율은 실측으로 읽힌다」는 규칙을 적으면서 실제 값을 예시로 쓰고 있었다.

---

## [1.3.0] — 2026-08-10

문서 제작 도구인데 README 에 결과물이 한 장도 없던 문제를 해결하였다.

### 더해짐

- **README 전후 대비** — 같은 내용을 규약 없이 한 번, 적용해 한 번 쓴 것을
  나란히 배치하였다. 스크린샷 3장(`docs/assets/`)과 규칙별 근거 대조표.
- `examples/before_after.md` — 전후 원고 전문과 「무엇이 왜 바뀌었나」 13행 대조표.
  각 변경에 해당 절 번호를 붙여 규칙이 어디서 작동했는지 추적 가능하다.
- `examples/build_before_after.py` — 같은 원고를 브라우저 기본 스타일과
  `korean-report-doc` 조판으로 각각 렌더한다. 문체와 조판의 기여를 분리해 보인다.
- 검사 3종 추가 — README 가 실재하지 않는 이미지를 가리키는지, 전후 예시가
  가상임을 밝히는지 확인한다.

### 참고

전후 예시의 주제는 **고객 문의 대응 체계 개선**(가상 B사)으로, 저장소의 다른
예시(반도체 계측)와 도메인이 다르다. 의도한 것이다 — README 첫 화면에서
도메인 용어가 나오면 진입장벽이 생기고, 주제가 다르면 규칙이 분야에
묶이지 않는다는 것이 저절로 드러난다.

---

## [1.2.0] — 2026-08-10

병렬 개선판(`korean-report-style.skill`)을 3방향 병합하였다.
구조·판정법은 새 버전에서, 예시는 가상 반도체 사례에서 가져왔다.

### 더해짐

- **§1.8 「어휘 선택 — 정밀도 기준」** — 신규 절.
  - 광의 고유어 → 협의 한자어 24행 (`넘긴다 → 전달·이관·위임`)
  - **판정법 — 목적어를 바꿔 끼워 본다.** 서로 다른 종류가 모두 자연스러우면 의미역이 넓다
  - 표기 층위 3단 — 식별자는 영문 원문, 개념은 영문 또는 정착 번역, 서술은 한자어
  - `SUCCESS`를 「성공」으로 적으면 코드와 문서가 어긋난다 (enum·필드명 불번역)
  - 정밀도 없는 격식체 제거 (`상기·하기·익일·요망`)
  - **교정안도 이 표를 통과해야 한다** — 규칙을 자기 자신에게 적용
- **§1.3 판정법** — 주어를 사람·물건으로 바꿔 자연스러우면 전용된 비유다.
  `본 문서는 …를 다룬다`를 약한 의인화로 잡아내는 항목을 포함한다.
- **§1.4 어체 혼용 금지** — 평서체와 합니다체를 한 문서에 섞지 않는다.
  필요하면 문서와 커버레터를 분리한다.
- **§6 판정 3단계** — 기계 치환 → 판정 적용 → 프레이밍 검토.
  「치환표는 예시이지 목록이 아니다」를 명시하였다.
- **§6 검사 범위 체크리스트** — 절 제목 · 표 좌우변 · 도해 라벨 · 본문 산문.
  본문만 보면 서술형 절 제목이 가장 자주 남는다.
- §7 「쉬운 말과 정밀한 말이 충돌할 때」 판단 항목.

### 바뀜

- 절 제목을 명사구로 통일 — `상대의 말로 시작한다` → `상대의 요청을 기점으로`.
  스킬이 자기 §1.1을 자신에게 적용한 것이다.
- 치환표 78 → 115건. §4 의인화 8 → 14, §7 어휘 선택 4 → 35(7.1·7.2·7.3으로 분할).
- 치환 건수 검사가 §7.1 「광의 → 협의」 표도 세도록 보강.

### 고침

- 병렬 개선판이 들여온 내부 API·필드명(`venueId` · `NavigationStarted` ·
  `QAB endpoint` · `minSdk`)을 가상 식별자로 교체하고 검사 목록에 추가하였다.
  실명은 아니지만 공개 저장소의 예시로는 부적절하다.
- 회귀 검사가 숨김 디렉터리(임시 추출 경로)를 스캔하던 문제.

---

## [1.1.0] — 2026-08-10

공개 준비. 예시에서 실제 사업 데이터를 걷어내고 가상 반도체 사례로 통일하였다.
판단 근거는 [결정 기록](docs/decisions/2026-08-10-example-domain-swap.md) 참조.

### 바뀜

- **예시 도메인 교체** — 실내측위에서 반도체 수율·계측으로. 가상 시나리오
  「A사 — 인라인 계측 체계 확립과 수율 개선」 하나로 저장소 전체를 통일하였다.
  치환표 78건 중 43건 교체, 35건 유지. **§1 어미 10건은 순수 문법이라 손대지 않았고,
  §3 구어체의 절반 이상도 도메인 무관이라 그대로 두었다. 규칙 자체는 바꾸지 않았다.**
- **§9 「바꾸지 않는 것」의 기준 강화** — 「옮기면 어색하다」에서 「옮기면 **틀린다**」로.
  `오버레이 → 정렬`, `스큐 → 편차` 는 부정확한 게 아니라 다른 말이다.
  각 용어에 무엇이 틀리는지 한 줄씩 붙였다.
- **§4 에 항목 하나 추가** — `킬러 디펙트가 다이를 죽인다`. 이름은 §9 로 남기고
  서술만 고친다는 두 규칙의 경계를 한 줄로 보여준다. 총계 77 → 78.
- `design.md` §4.3 의 문체 예시도 같은 시나리오로 교체. 스타일 규칙이 doc 스킬에
  복제돼 있던 자리다.
- 예시 문서에 배경 절(§0)을 추가. 반도체를 모르는 독자가 킬러·뉴슨스·빈닝을
  읽을 수 있게 한다.
- 예시 문서 표지에 `(가상 사례)` 명시.

### 고침

- **`fig_compare` 오용** — 예시가 `900초`와 `5/7`을 한 축에 올려 작은 값의 막대가
  보이지 않았다. 원본에서 물려받은 문제다. 예시를 초 단위로 통일하고,
  `figures.md` 에 「축을 공유하므로 단위를 섞지 않는다」를 명시하였다.
- **§1.4 어미 전역 치환의 위험** — 인용문·코드 블록을 건너뛰라는 단서를 추가.
  남의 말을 옮긴 자리까지 문어체로 바꾸면 인용이 아니게 된다.
- 치환 건수 검사가 §9 표(치환쌍이 아님)까지 세던 문제. 헤더가 「쓰지 않는다」·「찾기」인
  표만 센다.

### 더해짐

- `tests/test_no_real_data.py` — 20개 검사. 실재 조직·개인명, 이전 도메인 잔재,
  스킬 산문의 구체 날짜, 치환표의 소수점 백분율, 가상 사례 표기 누락을 막는다.
- `docs/decisions/` — 결정 기록(ADR). 검토한 후보 10개와 탈락 사유를 남긴다.

---

## [1.0.0] — 2026-08-10

전수조사에서 나온 결함을 일괄 수정하고 저장소를 정비한 첫 릴리스.
문서와 자산이 어긋나는 것을 검사로 묶었다.

### 고침

- **deck 인쇄가 paper 규칙에 덮이던 문제.** `deck_template.html` 이 같은
  `<style>` 블록을 두 번 담았고 뒤 블록이 paper 의 `@media print` 였다.
  뒤가 이겨 본문 11pt→9.8pt, h2 22pt→14pt 로 밀리고 hero 만 34pt 로 남았다.
- **deck 본문 구조가 paper 의 것이던 문제.** `<div class="doc">` 와 720px
  footer 가 들어 있어 full-bleed 타일이 원리적으로 불가능했다. 타일 구조와
  `EOD` 전면 타일로 교체하였다.
- **문서에만 있고 구현되지 않던 CSS.** `.gantt` 계열(`fig_gantt` 가 실제로
  방출하는 클래스), `.metric` 계열, deck 표지(`.cover` · `.hero` · `.rule` ·
  `.meta`)가 어느 템플릿에도 없었다.
- **`fig_cards` 좌표계 역전.** y-up 으로 계산한 배치를 뒤집지 않아 두 번째 행이
  첫 행 위에 그려지고, 헤더 바가 카드 아래에, 설명이 위로 쌓였다.
- **`fig_compare` 의 차이 구간 소실.** 부값이 주값보다 크면 사각형 폭이 음수가
  되어 렌더되지 않았다. 대소와 무관하게 그린다.
- **빌드가 실패하지 않던 문제.** `mathbuild.js` 가 수식 오류를 삼키고 exit 0
  이었다. `SKILL.md` §2.2 는 "빌드가 실패하게 한다"고 적혀 있었다.
- **런타임 CDN 의존.** 두 템플릿이 Pretendard 를 jsDelivr 에서 불러
  "자립형" 원칙(design.md §0.2 · §11)을 자기 자산이 위반하고 있었다.
- **문서·코드 불일치.** `design.md` 가 구버전 `%%D%%` 마커를 적고 있었고,
  `korean-report-style/SKILL.md` 는 존재하지 않는 `DOCUMENT.md` 를 가리켰으며,
  `INSTALL.md` 는 `install.sh` 가 지원하지 않는 Cursor 를 표에 넣었고,
  README 는 치환 목록을 "100건 이상"이라 했으나 실제는 77건이었다.
- 간트 마커가 막대 대비 12px 어긋나던 문제(라벨 폭만 더하고 gap 을 빠뜨림).
- 한 문서에 `fig_flow` 가 둘 이상이면 화살촉 `marker` id 가 충돌하던 문제.
- 문자열 치환에 `$&` 류 패턴이 해석될 수 있던 문제(함수 replacer 로 교체).
- `katex` 경로를 CWD 기준으로 찾던 문제(`require.resolve` 기반으로 교체).

### 바뀜

- **CSS 를 세 레이어로 분리.** `assets/css/base.css`(공통) ·
  `paper.css` · `deck.css`(모드). 템플릿은 치환 토큰만 담은 껍데기가 되었다.
  같은 규칙이 두 곳에 존재할 수 없으므로 덮어쓰기 사고가 구조적으로 불가능하다.
- **도해 색을 클래스로 방출.** hex 대신 `fi-*`(채움) · `st-*`(선)을 붙이고
  `base.css` 가 토큰에 잇는다. 다크 타일에서 토큰만 바뀌면 도해가 따라온다.
  design.md §5.1 의 "토큰을 상속한다"는 서술이 이제 사실이다.
- **폰트를 빌드 시점에 내장.** `mathbuild.js --font <woff2>` 로 Pretendard 를
  base64 `@font-face` 로 넣는다. 생략하면 경고 후 시스템 폰트로 폴백한다.
- `mathbuild.js` 가 `<html data-mode>` 를 보고 모드 CSS 를 고른다.
- KaTeX 폰트 선별을 클래스 기반으로 바꿔 실제로 쓰이는 계열만 남긴다.
- `figures.py` 가 라벨·제목·설명을 이스케이프한다. `tbl()` 셀은 배지·수식
  마커를 담아야 하므로 원시 HTML 로 남기고, `esc()` 를 함께 노출한다.
- 저장소를 `skills/` · `scripts/` · `tests/` · `examples/` 로 재배치.

### 없어짐

- `fig_cards(dark=...)` — 두 분기가 같은 값이라 아무 일도 하지 않았다.
  `title` · `sub` 는 받기만 하고 쓰지 않았으나 이제 실제로 렌더한다.

### 더해짐

- `tests/` — 125개 검사. 문서·자산 정합성, 도해 기하, 빌더 실패 계약,
  파이프라인 조립, 렌더 결과.
- `scripts/qa.py` — SKILL.md §4 의 기계 검사를 수행하고 실패하면 exit 1.
- `examples/build_example.py` — 파이프라인 참조 구현이자 회귀 화면.
- `scripts/pack-skills.sh` — `.skill` 생성과 `--verify` 대조.
- GitHub Actions CI, Apache-2.0 라이선스, 기여 안내.
