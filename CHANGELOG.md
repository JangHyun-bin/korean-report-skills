# 변경 이력

[Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르며
[유의적 버전](https://semver.org/lang/ko/)을 쓴다.

## [Unreleased]

## [1.18.1] — 2026-09-05

### 고쳐짐

- **설치기 터미널 출력이 평서체였다.** 「완료. 세션을 새로 시작하면 적용된다.」 를 냈다.
  §4.1 이 「바깥 독자에게 말을 거는 문서는 합니다체」로 정하였으나 그 선이 `.md` 까지만
  닿았다. `bin/install.js` 와 `scripts/install.sh` 의 문자열은 어떤 검사에도 들어가지
  않았고, 문서 넷이 합니다체로 옮겨 간 뒤에도 두 설치기만 남았다. 설치기는 문서보다 먼저
  사용자를 만난다.

### 더해짐

- `tests/test_cli_register.py` — 두 설치기의 문자열 리터럴에서 한국어 문장을 뽑아 합니다체
  종결을 요구한다. 판정은 `니다` 로 한다. 자모를 나열하면 `적용됩니다` 를 놓친다.
  세 번째 검사가 추출이 빈 목록을 내는 경우를 막는다 — 문장을 하나도 뽑지 못하면 통과해도
  의미가 없다.

## [1.18.0] — 2026-09-04

### 더해짐

- **`서식` 갈래.** 서식 결함은 마크업을 삭제하게 하고 문체 결함은 문장을 수정하게 한다.
  같은 갈래에 섞으면 「문장을 손대지 않고 서식만 정리」가 되지 않는다. `--tier 서식` 으로
  따로 돌린다.
- **이모지 검사.** 그림문자, 이모지 표현 선택자가 붙은 기호, 기본 표현이 이모지인 소수 기호만
  본다. 화살표와 체크 기호는 본문 기호로 남긴다. 이 저장소가 `→` 를 511회 쓰므로 그것까지
  잡으면 검사기를 끄게 된다.
- **굵은 라벨 목록 검사.** 목록 블록 세 항 이상이면서 **라벨이 자기 본문을 되풀이할 때만**
  낸다. 형태만으로 판정하면 같은 모양을 정당하게 쓰는 용어 사전이 걸린다.
- **제목 되풀이 검사.** 제목과 그다음 문단의 인접으로 판정한다.
- **대화 잔재 일곱 건.** 요청자에게 하는 말이 문서에 남은 것이다. 치환표 §10 이다.
- **군더더기 구문 아홉 건.** 검증된 일대일 대응이라 `--fix` 가 수정한다. 치환표 §11 이며
  `FIX_SECTIONS` 를 §1 하나에서 §1·§11 로 넓혔다.
- **밀도 규칙 둘.** 굵게 남발과 완충어 과잉이다. 임계값과 완충어 목록은 새 참조표
  `references/density.md` 에 있다. 임계값을 code 에 적으면 규칙이 두 곳에 생긴다.
- 검사 단위를 넷 더하였다 — 문단, 목록 블록, 제목 인접, 문장.

### 근거

- 굵게 남발 임계값은 이 저장소의 한국어 문서에서 측정하였다. 목록을 뺀 515문단에서 문단당
  굵게가 0개 350 · 1개 157 · 2개 6 · 3개 2 였다. 3이 정상 범위를 벗어나는 첫 값이다.
- 완충어가 둘 이상 겹친 문장은 저장소에 없었다. 임계값 2는 여기서 findings 를 내지 않으며,
  실제 겹침으로는 검증되지 않았다.
- 두 규칙 모두 `--heuristic` 에서만 본다. 임계값이 목표 장르 말뭉치가 아니라 이 저장소에서
  나온 값이다.

### 바뀜

- README 갈래 표가 네 개만 적고 있었다. `형태` 는 1.15.0 에 들어갔는데 표에 없었다.
  여섯 개를 전부 적고 건수도 맞추었다.

### 문서

- `docs/design` 에 AI slop 패턴 확장 명세를 더하였다. `blader/humanizer` 의 pattern 35건을
  기존 규칙과 대조하여 12건을 채택하고 세 단계로 갈랐다. 8월 연구 명세도 함께 편입하였다.

## [1.17.0] — 2026-09-04

### 고쳐짐

- **문체 혼용 검사가 한 번도 돌지 않았다.** `POLITE` 에 초성 ㅂ(U+1107) 을 적어 두었는데
  분석기는 `보입니다` 의 어미를 종성 ㅂ(U+11B8) 로 낸다. `습니다` 를 뺀 모든 합니다체가
  검사를 빠져나갔다. 저장소 자신의 README 가 합니다체 아홉 문장과 평서체 마흔한 문장을
  섞은 채 「문체 규약 위반 없음」을 받았다. 자모를 나열하지 않고 `니다` 로 판정한다.
- **돌리지 못한 검사를 통과한 검사처럼 인쇄하였다.** `kiwipiepy` 가 `pyproject` 에도 CI 에도
  선언되어 있지 않아 형태 tier 전체가 빈 목록을 냈다. 의존을 선언하고, 건너뛸 때는
  stderr 로 알린다.
- **어체를 줄 단위로 판정하였다.** 줄바꿈으로 잘린 토막의 끝을 종결 어미로 읽어
  멀쩡한 문서를 혼용으로 잡았다. 문단 단위로 이어 붙여 분석한다.
- **제목을 어체 집계에 넣었다.** 제목은 명사구이므로 어체를 가지지 않는다. `기여 안내` 가
  `안 + 내다` 로 분석되어 평서체 한 표를 더하고 있었다.

### 더해짐

- **§5 형용사 활용.** 형용사 어간에 동사 평서형 어미가 붙는 `필요한다` · `필요하지 않는다`
  를 검출한다. `하/XSA` 파생으로 한정한다 — 고유어 형용사는 동형어가 많아
  `압박으로 적지 않는다` 의 `적-`(기록하다)을 형용사로 오분석한다.
- **§6 종결형 어간.** 치환표 §7.1 은 용언을 평서체 문자열로 적어 두어 합니다체 문서가
  그 규칙들을 통째로 빠져나갔다. 같은 표에서 어간을 읽어 종결 어미와 무관하게 판정한다.
  규칙은 여전히 한 곳에만 적혀 있다. 여러 형태를 나열한 행은 문자열 규칙으로 둔다 —
  `둔다 · 두면` 을 어간으로 넓히면 `예외로 둔다` 같은 정당한 용법까지 걸린다.
- 형태소 회귀 검사 열세 건을 `tests/test_morph.py` 로 더하였다.

### 바뀜

- **README · CONTRIBUTING · SECURITY · INSTALL 을 합니다체로 옮겼다.** §4.1 에 선을 명시하였다 —
  바깥 독자에게 말을 거는 문서는 합니다체, 규칙과 기록은 평서체다. `CHANGELOG` 는
  평서체를 유지한다. 1.15.0 에서 이 네 파일을 평서체로 규정하였으나, 공개 안내 문서는
  독자에게 말을 거는 글이므로 어체를 뒤집는다.

## [1.16.0] — 2026-08-24

### 고쳐짐

- **typesetting 을 거친 HTML 에서 제목 검사가 돌지 않았다.** `lint` 가 `if not html`
  으로 §1.1 을 통째로 건너뛰고 있었다. 원고를 markdown 으로 쓰지 않고 생성기로
  조립하는 문서는 제목이 한 번도 검사되지 않는다. 실제로 서술형 제목 여섯 개가
  검사를 통과한 채 typesetting 까지 나갔다.
- **줄 단위 검사로는 생성기가 만든 제목을 놓친다.** 개행 없이 이어 붙이면 제목이
  긴 줄 가운데 묻혀 `$` 앵커에 걸리지 않는다. 요소 단위로 찾도록 바꾸었다.
  `<h1>`~`<h6>` 만 대상이며 도해는 SVG `text` 로 나가므로 영향받지 않는다.

### 바뀜

- `test_html_does_not_check_headings` 를 세 검사로 교체하였다. 기존 검사는
  「typesetting 을 거친 제목은 도해의 주장일 수 있다」를 근거로 들었으나,
  `figures.py` 는 `<h>` 태그를 내지 않는다. 도해가 제목으로 오인될 경로가 없다.


## [1.15.0] — 2026-08-23

### 더해짐

- 형태소 기반 문체 검사를 더하였다. 한국어는 교착어라 어간 하나에 활용형이 수십 개
  붙으므로 문자열 치환표는 활용형에서 샌다. 같은 위반을 활용형만 바꾼 14문장을
  검사하였을 때 치환표는 7문장을 통과시켰고 형태소 검사는 14문장을 전부 검출하였다.
- 닫힌 부류는 형태소로, 열린 부류는 판정법으로 나누었다. 동사 어간·단위 명사·종결
  어미는 목록으로 판정하고, 의인화 표현은 주어가 무정물인지 확인하여 후보로 올린다.
- 단위 명사는 허용 목록으로 판정한다. 세는 대상은 무한하지만 단위는 닫힌 부류다.
- 파일별 기대 어체를 규정하였다. README·CONTRIBUTING·SECURITY·INSTALL·CHANGELOG는
  평서체이며 다수·소수를 세지 않고 기대와 다른 어체를 전부 보고한다.
- 검사 갈래에 `형태`를 더하고 주의 어간은 `--all` 에서만 본다.

### 바뀜

- **README를 평서체로 되돌렸다.** 1.14.2 에서 공개 안내문을 합니다체로 통일하였으나,
  평서체를 규정하는 스킬의 안내문이 다른 어체를 쓰면 규약을 스스로 부정하는 것이 된다.
  섞임이 절 단위 분리가 아니라 절 안의 표류였던 점도 함께 확인하였다. 문장 59곳을
  고쳤고 코드 블록과 표는 건드리지 않았다.
- `SKILL.md` §1.4 에 저장소 문서의 어체를 명시하였다. 이전에는 README가 「대외 안내」와
  「정의서」 중 어디에 속하는지 규정되어 있지 않아 판단이 갈렸다.
- 두 검사기가 같은 자리를 잡으면 형태소 쪽만 남긴다. 같은 위반을 두 번 보고하면
  소음이 되고, 소음이 되면 검사기가 꺼진다.

### 고쳐짐

- 형태소 검사가 기존 문서에서 활용형 때문에 치환표를 빠져나가던 위반을 찾아
  함께 교정하였다 — 스킬 문서의 「지우면」·「갈리는」, 시각 규약 문서의 「다루고」,
  도해 규약 문서의 「나누거나」, 시각 정체성 문서의 「마크가 앉아」.

### 알려진 제약

- `kiwipiepy` 가 없으면 형태소 검사를 건너뛴다. 설치는 `python -m pip install kiwipiepy`.
- 어미 축약은 형태소로 판정하지 않는다. 분석기가 「했다」와 「하였다」를 같은
  형태소열로 정규화하므로 구분되지 않으며 표면형 검사가 담당한다.

## [1.14.2] — 2026-08-17

### 더해짐

- 폐기한 문서 제작 용어가 공개 문서와 소스에 다시 들어오지 않는지 저장소 단위로
  검사한다. Git 저장소와 npm 배포본에서 같은 검사를 실행한다.

### 바뀜

- README를 문제 중심의 훅, 두 스킬의 역할, 사용 예, 결과물, 설치, 검수 순서로
  재구성하였다. 공개 안내문은 합니다체로 통일하고 내부 규격 설명의 중복을 줄였다.
- 페이지와 요소의 배치는 `layout`, CSS·글꼴·수식을 반영한 HTML 생성 단계는
  `typesetting`으로 구분하였다. 저장소 전체의 이전 용어도 같은 기준으로 정리하였다.

## [1.14.1] — 2026-08-17

### 더해짐

- `korean-report-doc` 배포물에 `assets/qa.py`를 포함한다. 플러그인 설치본과 `.skill`
  압축 파일에서도 저장소 루트 없이 렌더 QA, PDF, 스크린샷 출력을 실행할 수 있다.
- 복사된 스킬이 작업 디렉터리의 KaTeX와 내장 QA를 사용하는지 회귀 검사한다.
- README와 INSTALL을 저장소 자체 문체 검사 대상에 포함한다.
- `npm pack --dry-run` 결과의 공개 진입점·script 대상·주요 문서 자산을 검사하고,
  OpenCode plugin을 실제 import하여 `skills.paths`와 두 스킬의 배포 여부를 확인한다.
- Git tag와 `package.json` version을 릴리스 전에 대조한다.
- GitHub Actions의 npm trusted publishing을 사용하여 npm 게시가 성공한 뒤에만
  GitHub Release를 생성한다. 재실행 시 기존 tarball과 현재 tag의 integrity도 대조한다.

### 고침

- `mathbuild.js`가 자신의 설치 경로에서만 KaTeX를 찾던 문제를 수정하였다. 작업
  디렉터리와 스킬 설치 경로를 순서대로 검사한다.
- 배포된 `SKILL.md`가 저장소 루트의 예제나 다른 스킬 내부 파일을 로컬 참조하지 않도록
  수정하고, 스킬 폴더 단위의 참조 정합성을 검사한다.
- `korean-report-doc`의 산출물 조건에서 무조건적인 본문 글꼴 내장과 동일한
  `typesetting` 결과 보장을 제거하고, 런타임 요구사항과 `--font` 적용 조건을
  명시하였다.
- README를 두 스킬의 역할, 지원 범위, 설치 경로, 산출물 계약, 품질 검사 순서로
  재구성하였다. 질문형 사례와 검증되지 않은 출력 보장은 제거하였다.
- 설치 직후의 자연어 사용 경로와 소스 저장소의 생성기 실행 경로를 분리하였다.
  본문 글꼴은 `--font` 지정 시에만 내장된다는 조건과 플랫폼별 갱신 방식을 명시하였다.
- npm tarball에 examples·tests·설계 문서·README 이미지·저장소 운영 문서를 포함하여
  공개 npm script와 README의 로컬 참조가 누락된 파일을 가리키지 않게 하였다. 테스트
  실행 중 생성되는 `__pycache__`와 `.pyc`는 배포에서 제외한다.
- npm script의 Python 호출은 Unix에서 `python3`, Windows에서 `py -3`을 우선 탐색하여
  `python` 별칭이 없는 환경에서도 같은 명령을 실행한다.
- Markdown 회귀 검사는 `.git`이 없는 npm 설치본에서 배포 파일을 직접 순회한다.
  게시된 tarball 안에서도 `npm test` 전체가 실행된다.
- npx 도움말에 남아 있던 「Codex는 plugin 체계가 없다」는 설명을 현재 설치 경로에
  맞게 교정하였다.

## [1.14.0] — 2026-08-17

### 더해짐

- **OpenCode plugin 지원.** `opencode.json` 의 `plugin` 배열에
  `"korean-report-skills"` 를 더하면 두 스킬이 로드된다. `.opencode/plugins/`
  의 `config` 훅이 스킬 디렉토리를 `skills.paths` 에 등록한다. npm 패키지로
  게시되며, 설치 시 파일 복사나 별도 설정이 필요 없다.

## [1.13.0] — 2026-08-15

문체 규칙이 적용 위치를 구분하지 않아 본문 요구사항까지 제목 규칙으로 검출하였고,
검사 대상이라고 명시한 일반 표는 구현에서 제외되어 있었다. 규칙의 적용 범위를 명시하고
software handoff와 저장소 단위 검사를 지원하도록 검사 계약을 확장한다.

### 더해짐

- 각 규칙에 안정적인 `rule_id`, `scope`, `literal|regex` 검출 방식을 부여하였다.
  기존 두 열 치환표는 `all`·`literal`로 읽어 사용자 정의 표의 하위 호환성을 유지한다.
- `prose`·`heading`·`table`·`html`·`all` 범위를 구분한다. `되어야 한다`는 제목에서만
  검토하므로 본문의 결정적 요구사항을 검사 통과 목적으로 약화할 필요가 없다.
- 일반 Markdown 표를 본문과 함께 검사한다. 규칙 설명용 표는 알려진 헤더 또는
  `<!-- style-teaching -->` 표식으로 제외한다.
- `~하는 방법`·`~하는 과정`·`~할 때`·`~할 것`·`~하기` 형태의 제목을 검출한다.
  `처리 방법`·`저장 과정`처럼 이미 간결한 명사구인 제목은 유지한다.
- 코드베이스 인수인계, architecture, runbook, API reference, model evaluation에서 자주
  나타나는 의인화·물리 은유를 7개 collocation 규칙으로 정리하였다. `--heuristic`에서만
  `의심` 갈래로 보고하고 자동 교정하지 않는다. bare verb는 규칙으로 추가하지 않았다.
- `--format github`와 SARIF 2.1.0 출력을 추가하였다. JSON에는 `rule_id`와
  `summary.total`·`by_tier`·`by_file`·`by_rule`을 추가하였다. 기존 `--json`과 루트 키는
  그대로 유지한다.
- 익명화된 software handoff fixture와 scope, 표, 제목, heuristic, 축약형, 출력 형식의
  회귀 검사를 추가하였다.

### 고침

- `통합돼 있다`·`호출돼야 한다`·`변경됐는가`·`검출됐는지`처럼 확인된 활용형을
  문어체로 자동 교정한다. `돼`·`됐` 단독 전역 치환과 문맥 의존 규칙의 자동 교정은 하지 않는다.
- 같은 HTML 줄에 일반 본문과 `<code>`가 함께 있을 때 code 내부까지 교정될 수 있던 경계를
  가시 텍스트 마스크 하나로 통합하였다.
- Node 25의 TAP 요약 기호 변경을 수용하여 README 검사 수 배지가 현재 Node와 CI의 Node 20에서
  모두 검증되도록 하였다.
- 일반 표 검사 활성화로 드러난 저장소 자체 문체 위반을 교정하고, 교육용 표를 명시적으로
  표시하였다.

## [1.12.0] — 2026-08-12

강조가 `b{font-weight:600}` 하나에 걸려 있었고, CI 는 우분투 한 대에서만 돌았고,
README 이미지 다섯 장 중 셋은 어떤 스크립트도 만들지 않았다. 셋을 함께 정리한다.

### 더해짐

- **본문 강조 두 층.** 지금까지 본문 강조를 나르는 것은 `b{font-weight:600}` 하나였고,
  16.5px 본문에서 400 → 600 은 약한 신호다. `<mark>` 를 더한다 — 액센트가 아니라
  **독자 층위**다. 액센트는 「이것이 요점이다」를, 강조는 「이 부분을 읽으라」를 말한다.
  절당 한 곳으로 제한하며, 제목·번호·링크에는 걸지 않는다. 이 경계가 무너지면
  `design.md` §1.1 이 금지하는 두 번째 액센트가 된다.
- **`<mark>` 는 글자색을 상속하지 않고 고정한다.** 다크 타일이 `--ink` 를 흰색으로
  뒤집으므로 상속하면 노랑 위 흰 글자가 된다. 다섯 안의 `typesetting` 결과를 비교해
  확인하였다 — 색을 물려받은 안은 다크에서 읽히지 않았고 박아 둔 안만 살아남았다.
  hex 를 박아 도해가 다크에서 사라지던 것과 같은 계통이라 검사로 고정한다.
- **italic 을 밝힌 라틴 구간으로 한정한다.** Pretendard 에 italic 자족이 없어 한글에
  걸면 브라우저 합성 oblique 이 나오고 획이 어긋난다. `<i lang="en">alpha</i>` 처럼
  밝힌 자리에만 걸리고, 밝히지 않으면 곧게 선다. 조용한 무동작이 기울어진 한글보다
  낫고, 처음 손대는 사람에게 규칙을 가르친다.
- 흑백 인쇄에서 노랑은 `--parchment` 와 같은 값으로 떨어져 code 칩과 구별되지 않는다.
  받아들인다 — 강조는 화면에서 읽는 사람을 유도하는 층이고 주장을 단독으로 나르지
  않는다. 인쇄본이 잃으면 안 되는 것은 배지나 콜아웃에 둔다.
- **CI 가 윈도에서도 검사를 돌린다.** 우분투 하나로만 돌던 동안 cp949 · 경로 구분자 ·
  CRLF 가 통째로 사각이었다. 검사기의 표준 입력 결함(1.11.0 「고침」)이 CI 를 초록으로
  통과하고 개발 기기에서 드러난 것이 그 결과다. **개발 환경이 CI 보다 엄격하면 CI 가
  하는 일이 없다.** 되돌림 실험으로 확인하였다 — 그 결함을 되살리면 윈도만 빨갛고
  우분투는 초록이다. 소요는 윈도 101초 · 우분투 86초로 15초 차이다.
- 두 러너가 모두 도는 것은 `ruff` · 빌더 검사 · `pytest` 다. README 자산 재생성과 스킬
  패키징은 우분투 한 곳에 남긴다 — 이미지는 글꼴 렌더가 OS 마다 달라 커밋된 PNG 와
  바이트로 대조되고, `pack-skills.sh` 는 `zip` · `unzip` 에 의존한다. 파이프라인
  자체는 `tests/test_build_e2e.py` 가 두 러너에서 PDF 출력까지 통과시킨다.
- `fail-fast` 를 끈다. 한쪽이 깨져도 다른 쪽 결과를 함께 봐야 OS 문제인지 가려진다.
- **README 이미지 다섯 장을 전부 CI 가 재생성해 대조한다.** 배너 두 벌만 이 경로에
  올라 있었고 전후 대비 세 장은 최초 커밋 이후 한 번도 갱신되지 않았다. 어떤 스크립트도
  그 셋을 만들지 않았으니 손으로 찍어 넣은 자산이었다 — 시각 정체성 설계 문서가 스스로
  「손으로 갱신하는 저장소는 반드시 어긋난다」고 적어 둔 바로 그 상태다.
  `scripts/build-banner.py` 가 셋을 함께 맡으면서 이름이 배너만 가리키게 되었으므로
  `scripts/build-shots.py` 로 바꾸었다. **이제 손으로 갱신하는 이미지가 없다.**
- 검사 하나를 더한다 — README 가 싣는 PNG 가 전부 CI 의 대조 목록에 있는지 본다.
  하나라도 빠지면 그것부터 낡기 때문이다.

## [1.11.0] — 2026-08-11

문체 규약이 권고로만 있었다. 치환표 115행을 모델이 읽고 지켜 주기를 바라는 구조였고,
지켰는지 확인할 방법이 사용자에게 없었다. 검사기를 스킬에 실어 보낸다.

### 더해짐

- **문체 검사기** (`korean-report-style/assets/lint.py`) — 치환표를 읽어 문서의
  위반을 줄·열로 낸다. 마크다운·HTML·표준 입력을 받고, `--json` 으로 기계가 읽는
  형식을 낸다. 규칙 목록을 따로 두지 않으므로 표를 고치면 검사도 함께 바뀐다.
  의존성은 없다.
- **세 갈래 보고** — 치환표 §1 이 스스로 「기계적 전역 치환이 가능하다」고 규정하므로
  어미는 `고침` 으로, 문맥이 필요한 §2~§8 은 `검토` 로, 절 제목은 `제목` 으로 가른다.
  `넘긴다` 를 무조건 결함으로 부르면 검사기가 소음이 되고, 소음이 되면 꺼진다.
- **`--fix`** — §1 어미만 파일에 직접 치환한다. 갈래가 여럿인 제안
  (`전달한다 · 이관한다 · 위임한다`)은 자동으로 적용하지 않는다.
- **`SKILL.md` §6 이 검사기를 절차에 넣는다** — 손으로 훑지 않는다. 종료 코드가
  0 이 아니면 아직 끝난 것이 아니며, 검토 갈래를 남기고 내밀 때는 이유를 말한다.

### 고침

- **의문형 절 제목이 새어 나가고 있었다.** 종결형(`다`·`까`·`~인가`)만 보던 규칙은
  `무엇이 달라지나` 를 놓쳤다. 이 검사가 만들어진 계기가 바로 그 제목이었다.
  「나」로 끝나는 명사가 있어 어미를 넓힐 수 없으므로 의문사 시작을 함께 본다.
- **저장소 자신의 검사가 배포본을 쓰도록 바꾸었다.** `test_own_prose.py` 가 같은
  논리를 따로 구현하고 있었다. 사용자가 받는 것과 저장소가 자신에게 적용하는 것이
  갈라지면 배포본 쪽이 먼저 낡는다.
- **치환표 §7.1 의 한 행이 잘려 있었다.** `는다 · 늘어난다` 는 홑 어미라 규칙으로
  쓸 수 없다. 오른쪽 열(`향상된다 · 증가한다 · 연장된다`)과 이웃 행의 형태로 보아
  `늘린다 · 늘어난다` 가 원형이다. 검사기를 붙이기 전까지는 통째로 한 문자열이라
  아무것도 걸지 않아 드러나지 않았다.
- **한 칸에 활용형이 여럿인 행이 어느 쪽도 잡지 못하고 있었다.** `올라간다 · 내려간다`
  를 통째로 한 규칙으로 두면 두 표현 다 새어 나간다. 갈라서 각각을 규칙으로 만든다.
  다만 갈라 나온 두 글자 조각은 버린다 — `상기 · 하기 · 여히` 의 `하기` 를
  「실행하기」·「하기 전에」와 가르려면 형태소 분석이 필요하다.
- **참조 검사가 `tests/` 와 `examples/` 를 탐색하지 않았다.** 문서가 검사 파일을
  가리키면 없는 파일로 취급되었다.
- **구분선 위의 빈 줄이 사라지면 윗줄이 제목이 된다.** README 캡션이 실제로
  `<h2>` 로 부풀었다. 마크다운의 setext 제목 문법이라 눈으로는 티가 나지 않는다.
  이 저장소는 제목을 전부 `#` 로 쓰므로 setext 는 언제나 사고다 — 검사로 막는다.
- **윈도에서 검사기가 표준 입력을 잘못 해독하고 있었다.** `stdout` 과 `stderr` 만
  utf-8 로 돌려놓고 `stdin` 을 빠뜨렸다. 윈도 기본 표준 입력은 cp949 이므로,
  검사기가 스스로 용법으로 적어 둔 `cat 원고.md | lint.py -` 경로에서 한글이 깨져
  규칙에 하나도 걸리지 않고 「위반 없음」과 종료 코드 0 을 냈다. **못 잡는 것보다
  틀리게 통과시키는 쪽이 나쁘다.** CI 가 우분투 하나뿐이라 드러나지 않았다.
- **`qa.py` 의 죽은 분기를 걷어냈다.** 조건이 한 항에 0 을 곱한 뒤 본문이 `pass`
  였다. 실제 검사는 바로 다음 줄이 한다.
- **재배치 때 따라가지 않은 경로 셋.** `SECURITY.md` 가 설치자에게 건넨 확인 명령과
  결함 신고 서식의 재현 예시가 `skills/` 를 가리키는데, 그 자리는 1.9.0 에서
  `plugins/korean-report/skills/` 로 옮겼다. 참조 검사가 확장자 있는 토큰만 보고
  마크다운만 열어 둘 다 통과하였다. `INSTALL.md` 는 플러그인을 임시로 끄는 방법으로
  설치본을 개인 스킬 폴더로 옮기라 하였는데 그곳도 적재 경로다 — Installed 탭과
  `--remove` 로 바꾸었다.

## [1.10.0] — 2026-08-10

첫 화면에 시각 앵커가 없었다 — 제목과 문단으로 바로 시작했다. 로고와 배너를
갖추고, 배너가 실제 문서에서 잘라낸 자산이라 문서가 바뀌면 CI 가 낡음을 잡게 했다.

### 더해짐

- **로고 세 벌** (`docs/assets/logo.svg` · `logo-sm.svg` · `favicon.svg`) —
  paper 모드 절 표제의 시각 서명을 크기별로 잘라낸 축소 3단. viewBox 를
  `0 0 100 100` 으로 고정하고, GitHub 라이트·다크에서 같은 파일이 따라가도록
  `prefers-color-scheme: dark` 로 색을 전환한다. `<img src>` 로 불릴 때는
  `currentColor` 가 통하지 않아 파일 안에 직접 둔다.
- **배너 생성기** (`scripts/build-banner.py`) — 예시 문서 세 조각(paper 표지·
  deck 다크 타일·paper 본문)을 Playwright 로 캡처해 겹치고, 라이트·다크 두 벌을
  합성한다. 재실행해도 바이트가 같다.
- **README 헤더** — 워드마크·뱃지 행 아래 `<picture>` 로 테마별 배너를 얹었다.
- **CI 가 배너 낡음을 잡는다** — 「README 전후 대비 자산 빌드」 단계 뒤에
  `scripts/build-banner.py` 를 다시 돌리고 `git diff --exit-code` 로 커밋된
  PNG 와 대조한다. 문서가 바뀌었는데 배너를 갱신하지 않으면 CI 가 실패한다.

### 고침

- **「Codex 는 플러그인 체계가 없다」는 설명이 틀렸다.** Codex 에는 마켓플레이스가
  있고, 플러그인·마켓플레이스 선언 파일을 찾을 때 자기 경로에 이어
  `.claude-plugin/` 도 본다. 이 저장소는 파일을 하나도 더하지 않은 채 이미
  Codex 마켓플레이스로 동작한다 — `codex plugin marketplace add` 와
  `codex plugin add` 로 실제 설치되는 것을 확인하였다. README 와 INSTALL 을 고쳤다.
  다만 `.claude-plugin/` 인식이 영구 보장인지는 문서에 명문화되어 있지 않다.

### 빠짐

- **GitHub Pages** — 사이트와 배포 워크플로, 색인 HTML, 저장소 안의 모든 링크를
  걷어냈다. `favicon.svg` 는 축소 3단의 최소판이므로 자산만 남긴다.
  예시 문서를 실물로 공개하던 자리인데, 배너가 결과물을 이미 보여주고
  README 에 전후 대비도 있어 같은 말을 두 번 하고 있었다.
  결과물을 직접 보려면 CONTRIBUTING.md 의 「눈으로 봐야 하는 것」 절이 적은 세 줄을 실행한다
  — 빌드 스크립트만으로는 `typesetting` 전 원본이 나온다.

## [1.9.0] — 2026-08-10

설치 경로를 현대적인 방식으로 바꾸었다. `bash scripts/install.sh` 한 줄만 안내하고
있었는데, Claude Code 는 플러그인 마켓플레이스가 표준이고 갱신도 자동이다.

### 더해짐

- **Claude Code 플러그인 마켓플레이스** — 저장소가 곧 마켓플레이스다.

  ```
  /plugin marketplace add JangHyun-bin/korean-report-skills
  /plugin install korean-report@korean-report-skills
  ```

  `.claude-plugin/marketplace.json` 과 `plugins/korean-report/.claude-plugin/plugin.json`
  을 두었다. 두 스킬은 함께 쓰이므로 플러그인 하나에 담았다.
  마켓플레이스를 등록해 두면 갱신이 자동으로 따라온다.

- **npx 설치** — 플러그인 체계가 없는 도구(Codex · Cursor)와, 파일로 두고 싶을 때.

  ```
  npx github:JangHyun-bin/korean-report-skills
  npx github:JangHyun-bin/korean-report-skills --project
  npx github:JangHyun-bin/korean-report-skills --remove
  ```

  게시 없이 GitHub 에서 바로 실행된다. `--remove` 로 되돌릴 수 있다.

### 바뀜

- **저장소 배치를 플러그인 규격에 맞추었다** — `skills/` 가
  `plugins/korean-report/skills/` 로 이동하였다. 공식 문서가 규정하는 배치이며,
  루트를 플러그인으로 쓰는 방식은 문서에 없어 채택하지 않았다.
- `scripts/install.sh` 는 node 가 없는 환경을 위해 남긴다. 안내 순서에서는 뒤로 옮겼다.

---

## [1.8.0] — 2026-08-10

### 더해짐

- **저장소 자신의 산문을 검사한다** (`tests/test_own_prose.py`).
  `CONTRIBUTING` 과 §6 이 「본문 산문 — 규칙을 서술하는 문장 자신이 규칙을
  위반하는 경우가 있다」를 검사 범위에 넣어 두고도 저장소 자신은 아무도 보지
  않았다. 실제로 README 절 제목이 의문형이었고 바로 아래 표에서 그 형태를
  나쁜 예로 들고 있었다.

  **규칙 목록을 따로 만들지 않는다.** `substitutions.md` 의 「쓰지 않는다」 열을
  읽어 그대로 검사에 쓴다. 표를 고치면 검사도 함께 바뀐다.

  검사 대상은 규약이 적용되는 장르뿐이다 — `skills/` 와 `docs/decisions/`.
  README · INSTALL · CONTRIBUTING · Pages 는 대상이 아니며 그 이유를 README 에
  명시하였다.

### 고침

검사를 넣자 저장소 자신에서 **위반 28곳**이 나왔다.

- §1.4 어미 9곳 — 결정 기록의 `했다` · `했던`
- §1.8 광의 고유어 15곳 — `다룬다`(2) · `만든다`(2) · `넣는다`(2) · `맞춘다`(3) ·
  `나눈다` · `바꾼다`(2) · `고친다` · `막는다`
- §1.1 서술형·의문형 절 제목 4곳 —
  `파일이 필요한가` → `산출물 필요 여부`,
  `QA — 렌더링해서 확인한다` → `QA — 렌더 후 검사`,
  `좌표는 Python 에서 계산한다` → `좌표 계산의 위치 — 생성기`,
  `무엇을 유지했는가` → `유지한 범위`

---

## [1.7.0] — 2026-08-10

첫 화면을 문제 중심으로 다시 썼다. 규약을 가르치는 저장소가 정작 자기 문서를
알아보기 어렵게 써 두었고, 자기 규약도 지키지 않고 있었다.

### 바뀜

- **README 를 「불만 → 해결」 구조로 재작성.** 이전에는 「정의 → 설치 → 결과」
  순서라 처음 온 사람이 왜 필요한지 알 수 없었다. 이제 독자가 실제로 겪는 불만
  다섯 가지를 절 제목으로 쓰고 각각에 답한다 — 디자인, 번역체, 뭉뚱그린 단어,
  확정된 것처럼 쓴 숫자, 사고 보고서처럼 읽히는 프레이밍.
- **Pages 첫 화면에 맥락과 관찰 지침 추가.** 이전에는 「예시 문서」라는 제목뿐이라
  검색으로 들어온 사람이 무엇의 예시인지 알 수 없었다. 무엇을 확인해 볼지
  네 가지를 적었다 — 소스 보기 · 인쇄 미리보기 · 배지 · 확대.
- **About 정리** — 설명을 문제 중심으로 바꾸고, 홈페이지 URL(Pages)과
  토픽 8개를 설정하였다. 토픽이 0개여서 검색에 노출되지 않았다.

### 고침

저장소 자신의 문서가 스킬의 규약을 어기고 있었다.

- `## 무엇이 달라지나` — §1.1 이 금지하는 의문형 제목. **바로 아래 표에서
  「문의 대응이 왜 느려졌나」를 나쁜 예로 들면서 같은 화면에서 그 형태를 쓰고 있었다.**
- §1.8 광의 고유어 6곳 — `잡고` · `막는다` · `붙인다` · `돈다` · `걸린다` · `담은`
- §1.3 물리 은유 — `파이프라인을 돈다`
- §1.2 구어체 — `얇은 껍데기` · `둘이 함께 걸린다`

### 참고

README 의 절 제목은 여전히 의문형·구어체다. **의도한 것이며 그 사실을 README 에
명시하였다.** 규약의 대상은 보고서·협의 자료이고 README 는 처음 온 사람이 자기 문제를
알아보게 하는 글이다. 스킬 자신이 §7 에서 「기계적으로 적용하면 안 되는 경우」를
규정한다. 규약이 적용된 결과물은 Pages 의 실물 문서에서 확인한다.

---

## [1.6.0] — 2026-08-10

### 더해짐

- **`scripts/new-document.py`** — 새 문서 생성기의 뼈대를 만든다.
  만들어진 `.py` 는 **그 자체로 생성 → mathbuild → QA 를 한 번에 돈다.**
  `SKILL.md` §2 를 처음부터 읽지 않아도 첫 문서가 나오게 하는 것이 목적이다.
  이미 있는 파일은 덮어쓰지 않는다.

  ```bash
  python scripts/new-document.py --title "문서 제목" --mode paper
  python 문서_제목.py
  ```

- 스캐폴드가 자기 QA 를 통과하는지 검사한다. 처음 쓰는 사람이 가장 먼저
  실행하는 것이라 여기서 실패하면 첫인상이 실패로 시작한다.
  실제로 deck 쪽 치환 자리 수가 어긋나 캡션이 밀린 것을 이 검사가 잡았다.

---

## [1.5.0] — 2026-08-10

### 더해짐

- **deck 인쇄 오버플로 검사** — 타일이 한 쪽(A4 가로 210mm)을 넘으면 `qa.py` 가
  실패하고 어느 타일이 몇 px 넘쳤는지 알린다. `.tile` 은 인쇄 시 `overflow:hidden`
  이라 넘친 내용이 **조용히 잘린다** — deck 에서 가장 흔한 실패인데
  유일하게 자동 검사가 없던 자리다. 일부러 과밀한 타일을 만들어 잡히는 것을 확인하였다.
- **GitHub Pages** — 예시 문서를 실물로 공개한다. 스크린샷과 달리 파일 하나로
  완결된다는 것을 소스 보기로 직접 확인할 수 있다.
  https://janghyun-bin.github.io/korean-report-skills/

### 고침

- `qa.py` 가 **파일명으로 모드를 추측**하던 문제. `deck` 이 이름에 없는 파일은
  paper 로 취급되어 인쇄 검사가 건너뛰어졌다. 이제 `<html data-mode>` 를 읽고,
  값이 없거나 알 수 없으면 실패한다.

---

## [1.4.0] — 2026-08-10

공개 준비 마무리 — 라이선스 고지와 저장소 위생.

### 더해짐

- **생성 문서가 내장 글꼴의 라이선스를 스스로 고지한다.** 글꼴을 base64 로 넣는 것은
  그 사본을 문서와 함께 배포하는 것이므로 OFL·MIT 모두 고지 동반을 요구한다.
  KaTeX(MIT)는 항상, Pretendard(OFL 1.1)는 `--font` 로 내장했을 때만 실린다.
- `SECURITY.md` — 이 저장소의 특수한 위험(에이전트가 읽고 따르는 지시문)을 명시하고
  비공개 신고 경로를 안내한다. 설치자가 직접 확인할 수 있는 명령도 함께 적었다.
- 이슈 템플릿 2종 — 결함 신고, 규약 제안. 규약 제안 템플릿은
  「판정법으로 잡히는지 먼저 확인」을 첫 안내로 둔다. 표를 무한히 늘리지 않기 위해서다.
- PR 템플릿 — 「무엇을 왜」와 확인 목록. 다크 타일 육안 확인 항목을 포함한다.
- **버전 동기화 검사** — `package.json` · `pyproject.toml` · `CHANGELOG` 최신 항목이
  같은지 확인한다. 지금까지 우연히 맞아 있었을 뿐 검사가 없었다.
  릴리스 태그가 어긋나면 잘못된 버전이 배포된다.
- 저장소 위생 파일 존재 검사.

### 바뀜

- **금지 실명을 평문으로 두지 않는다.** 금지어 목록에 이름을 적으면 그 목록 자체가
  노출이다. 낱말 sha256 해시로 대조하도록 바꾸고, 이름을 몰라도 잡히도록
  법인 표기(`㈜` · `주식회사` · `Ltd.`) 형태 검사를 함께 두었다.
  검사 범위는 `LICENSE` · `NOTICE`(저작권 표기가 정상인 곳)를 제외한 전부다 —
  루트 문서를 빼면 거기로 실명이 새어 들어온다.
- 문서에 남아 있던 실측 수치 리터럴을 일반 표현으로 교체하였다.
  「소수점 백분율은 실측으로 읽힌다」는 규칙을 적으면서 실제 값을 예시로 쓰고 있었다.

---

## [1.3.0] — 2026-08-10

문서 제작 도구인데 README 에 결과물이 한 장도 없던 문제를 해결하였다.

### 더해짐

- **README 전후 대비** — 같은 내용을 규약 없이 한 번, 적용해 한 번 쓴 것을
  나란히 배치하였다. 스크린샷 3장(`docs/assets/`)과 규칙별 근거 대조표.
- `examples/before_after.md` — 전후 원고 전문과 「무엇이 왜 바뀌었나」 13행 대조표.
  각 변경에 해당 절 번호를 붙여 규칙이 어디서 작동했는지 추적 가능하다.
- `examples/build_before_after.py` — 같은 원고를 브라우저 기본 스타일과
  `korean-report-doc`으로 각각 렌더한다. 문체와 `typesetting`의 기여를 분리해 보인다.
- 검사 3종 추가 — README 가 실재하지 않는 이미지를 가리키는지, 전후 예시가
  가상임을 밝히는지 확인한다.

### 참고

전후 예시의 주제는 **고객 문의 대응 체계 개선**(가상 B사)으로, 저장소의 다른
예시(반도체 계측)와 도메인이 다르다. 의도한 것이다 — README 첫 화면에서
도메인 용어가 나오면 진입장벽이 생기고, 주제가 다르면 규칙이 분야에
묶이지 않는다는 것이 저절로 드러난다.

---

## [1.2.0] — 2026-08-10

병렬 개선판(`korean-report-style.skill`)을 3방향 병합하였다.
구조·판정법은 새 버전에서, 예시는 가상 반도체 사례에서 가져왔다.

### 더해짐

- **§1.8 「어휘 선택 — 정밀도 기준」** — 신규 절.
  - 광의 고유어 → 협의 한자어 24행 (`넘긴다 → 전달·이관·위임`)
  - **판정법 — 목적어를 바꿔 끼워 본다.** 서로 다른 종류가 모두 자연스러우면 의미역이 넓다
  - 표기 층위 3단 — 식별자는 영문 원문, 개념은 영문 또는 정착 번역, 서술은 한자어
  - `SUCCESS`를 「성공」으로 적으면 코드와 문서가 어긋난다 (enum·필드명 불번역)
  - 정밀도 없는 격식체 제거 (`상기·하기·익일·요망`)
  - **교정안도 이 표를 통과해야 한다** — 규칙을 자기 자신에게 적용
- **§1.3 판정법** — 주어를 사람·물건으로 바꿔 자연스러우면 전용된 비유다.
  `본 문서는 …를 다룬다`를 약한 의인화로 잡아내는 항목을 포함한다.
- **§1.4 어체 혼용 금지** — 평서체와 합니다체를 한 문서에 섞지 않는다.
  필요하면 문서와 커버레터를 분리한다.
- **§6 판정 3단계** — 기계 치환 → 판정 적용 → 프레이밍 검토.
  「치환표는 예시이지 목록이 아니다」를 명시하였다.
- **§6 검사 범위 체크리스트** — 절 제목 · 표 좌우변 · 도해 라벨 · 본문 산문.
  본문만 보면 서술형 절 제목이 가장 자주 남는다.
- §7 「쉬운 말과 정밀한 말이 충돌할 때」 판단 항목.

### 바뀜

- 절 제목을 명사구로 통일 — `상대의 말로 시작한다` → `상대의 요청을 기점으로`.
  스킬이 자기 §1.1을 자신에게 적용한 것이다.
- 치환표 78 → 115건. §4 의인화 8 → 14, §7 어휘 선택 4 → 35(7.1·7.2·7.3으로 분할).
- 치환 건수 검사가 §7.1 「광의 → 협의」 표도 세도록 보강.

### 고침

- 병렬 개선판이 들여온 내부 API·필드명(`venueId` · `NavigationStarted` ·
  `QAB endpoint` · `minSdk`)을 가상 식별자로 교체하고 검사 목록에 추가하였다.
  실명은 아니지만 공개 저장소의 예시로는 부적절하다.
- 회귀 검사가 숨김 디렉터리(임시 추출 경로)를 스캔하던 문제.

---

## [1.1.0] — 2026-08-10

공개 준비. 예시에서 실제 사업 데이터를 걷어내고 가상 반도체 사례로 통일하였다.

### 바뀜

- **예시 도메인 교체** — 실내측위에서 반도체 수율·계측으로. 가상 시나리오
  「A사 — 인라인 계측 체계 확립과 수율 개선」 하나로 저장소 전체를 통일하였다.
  치환표 78건 중 43건 교체, 35건 유지. **§1 어미 10건은 순수 문법이라 손대지 않았고,
  §3 구어체의 절반 이상도 도메인 무관이라 그대로 두었다. 규칙 자체는 바꾸지 않았다.**
- **§9 「바꾸지 않는 것」의 기준 강화** — 「옮기면 어색하다」에서 「옮기면 **틀린다**」로.
  `오버레이 → 정렬`, `스큐 → 편차` 는 부정확한 게 아니라 다른 말이다.
  각 용어에 무엇이 틀리는지 한 줄씩 붙였다.
- **§4 에 항목 하나 추가** — `킬러 디펙트가 다이를 죽인다`. 이름은 §9 로 남기고
  서술만 고친다는 두 규칙의 경계를 한 줄로 보여준다. 총계 77 → 78.
- `design.md` §4.3 의 문체 예시도 같은 시나리오로 교체. 스타일 규칙이 doc 스킬에
  복제돼 있던 자리다.
- 예시 문서에 배경 절(§0)을 추가. 반도체를 모르는 독자가 킬러·뉴슨스·빈닝을
  읽을 수 있게 한다.
- 예시 문서 표지에 `(가상 사례)` 명시.

### 고침

- **`fig_compare` 오용** — 예시가 `900초`와 `5/7`을 한 축에 올려 작은 값의 막대가
  보이지 않았다. 원본에서 물려받은 문제다. 예시를 초 단위로 통일하고,
  `figures.md` 에 「축을 공유하므로 단위를 섞지 않는다」를 명시하였다.
- **§1.4 어미 전역 치환의 위험** — 인용문·코드 블록을 건너뛰라는 단서를 추가.
  남의 말을 옮긴 자리까지 문어체로 바꾸면 인용이 아니게 된다.
- 치환 건수 검사가 §9 표(치환쌍이 아님)까지 세던 문제. 헤더가 「쓰지 않는다」·「찾기」인
  표만 센다.

### 더해짐

- `tests/test_no_real_data.py` — 20개 검사. 실재 조직·개인명, 이전 도메인 잔재,
  스킬 산문의 구체 날짜, 치환표의 소수점 백분율, 가상 사례 표기 누락을 막는다.
- `docs/decisions/` — 결정 기록(ADR). 검토한 후보 10개와 탈락 사유를 남긴다.

---

## [1.0.0] — 2026-08-10

전수조사에서 나온 결함을 일괄 수정하고 저장소를 정비한 첫 릴리스.
문서와 자산이 어긋나는 것을 검사로 묶었다.

### 고침

- **deck 인쇄가 paper 규칙에 덮이던 문제.** `deck_template.html` 이 같은
  `<style>` 블록을 두 번 담았고 뒤 블록이 paper 의 `@media print` 였다.
  뒤가 이겨 본문 11pt→9.8pt, h2 22pt→14pt 로 밀리고 hero 만 34pt 로 남았다.
- **deck 본문 구조가 paper 의 것이던 문제.** `<div class="doc">` 와 720px
  footer 가 들어 있어 full-bleed 타일이 원리적으로 불가능했다. 타일 구조와
  `EOD` 전면 타일로 교체하였다.
- **문서에만 있고 구현되지 않던 CSS.** `.gantt` 계열(`fig_gantt` 가 실제로
  방출하는 클래스), `.metric` 계열, deck 표지(`.cover` · `.hero` · `.rule` ·
  `.meta`)가 어느 템플릿에도 없었다.
- **`fig_cards` 좌표계 역전.** y-up 으로 계산한 배치를 뒤집지 않아 두 번째 행이
  첫 행 위에 그려지고, 헤더 바가 카드 아래에, 설명이 위로 쌓였다.
- **`fig_compare` 의 차이 구간 소실.** 부값이 주값보다 크면 사각형 폭이 음수가
  되어 렌더되지 않았다. 대소와 무관하게 그린다.
- **빌드가 실패하지 않던 문제.** `mathbuild.js` 가 수식 오류를 삼키고 exit 0
  이었다. `SKILL.md` §2.2 는 "빌드가 실패하게 한다"고 적혀 있었다.
- **런타임 CDN 의존.** 두 템플릿이 Pretendard 를 jsDelivr 에서 불러
  "자립형" 원칙(design.md §0.2 · §11)을 자기 자산이 위반하고 있었다.
- **문서·코드 불일치.** `design.md` 가 구버전 `%%D%%` 마커를 적고 있었고,
  `korean-report-style/SKILL.md` 는 존재하지 않는 `DOCUMENT.md` 를 가리켰으며,
  `INSTALL.md` 는 `install.sh` 가 지원하지 않는 Cursor 를 표에 넣었고,
  README 는 치환 목록을 "100건 이상"이라 했으나 실제는 77건이었다.
- 간트 마커가 막대 대비 12px 어긋나던 문제(라벨 폭만 더하고 gap 을 빠뜨림).
- 한 문서에 `fig_flow` 가 둘 이상이면 화살촉 `marker` id 가 충돌하던 문제.
- 문자열 치환에 `$&` 류 패턴이 해석될 수 있던 문제(함수 replacer 로 교체).
- `katex` 경로를 CWD 기준으로 찾던 문제(`require.resolve` 기반으로 교체).

### 바뀜

- **CSS 를 세 레이어로 분리.** `assets/css/base.css`(공통) ·
  `paper.css` · `deck.css`(모드). 템플릿은 치환 토큰만 담은 껍데기가 되었다.
  같은 규칙이 두 곳에 존재할 수 없으므로 덮어쓰기 사고가 구조적으로 불가능하다.
- **도해 색을 클래스로 방출.** hex 대신 `fi-*`(채움) · `st-*`(선)을 붙이고
  `base.css` 가 토큰에 잇는다. 다크 타일에서 토큰만 바뀌면 도해가 따라온다.
  design.md §5.1 의 "토큰을 상속한다"는 서술이 이제 사실이다.
- **폰트를 빌드 시점에 내장.** `mathbuild.js --font <woff2>` 로 Pretendard 를
  base64 `@font-face` 로 넣는다. 생략하면 경고 후 시스템 폰트로 폴백한다.
- `mathbuild.js` 가 `<html data-mode>` 를 보고 모드 CSS 를 고른다.
- KaTeX 폰트 선별을 클래스 기반으로 바꿔 실제로 쓰이는 계열만 남긴다.
- `figures.py` 가 라벨·제목·설명을 이스케이프한다. `tbl()` 셀은 배지·수식
  마커를 담아야 하므로 원시 HTML 로 남기고, `esc()` 를 함께 노출한다.
- 저장소를 `skills/` · `scripts/` · `tests/` · `examples/` 로 재배치.

### 없어짐

- `fig_cards(dark=...)` — 두 분기가 같은 값이라 아무 일도 하지 않았다.
  `title` · `sub` 는 받기만 하고 쓰지 않았으나 이제 실제로 렌더한다.

### 더해짐

- `tests/` — 125개 검사. 문서·자산 정합성, 도해 기하, 빌더 실패 계약,
  파이프라인 조립, 렌더 결과.
- `scripts/qa.py` — SKILL.md §4 의 기계 검사를 수행하고 실패하면 exit 1.
- `examples/build_example.py` — 파이프라인 참조 구현이자 회귀 화면.
- `scripts/pack-skills.sh` — `.skill` 생성과 `--verify` 대조.
- GitHub Actions CI, Apache-2.0 라이선스, 기여 안내.
